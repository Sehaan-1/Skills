---
description: Rampart (system design) — interview, design, review, estimate, decide, ood, or study (auto-picks mode)
---

Follow the rampart skill (system design). Locate its package root — typically `.claude/skills/rampart/` — and open `SKILL.md`.

1. Resolve the mode from the Invocation section using the user request below; if ambiguous, skim `references/examples.md` in the package root.
2. Load only that mode's files (progressive disclosure — do not read the whole package).
3. Run the appropriate flow (4-step interview by default) and close with the checklist/rubric when the mode calls for it.

User request: $ARGUMENTS
